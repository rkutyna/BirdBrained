You are one cycle inside an autonomous autoresearch loop for a bird species classifier.

Read `{program_md}` for full instructions. Your optimization target is `top1_val_acc`.

## Scope

- Modify only `{train_py}`.
- Read `{autoresearch_log}` first if it exists.
- If `{autoresearch_log}` is missing or too short to be informative, consult
  `{fallback_summary}` for historical context.

## This cycle

1. Read the experiment history to see what has been tried and what the current best is.
2. Propose exactly one change with a clear hypothesis.
3. Edit `{train_py}` -- update the CONFIGURATION section and set NOTES to describe the change.
4. Run `python train.py` and wait for it to finish.
5. Read the structured summary (`---` block) or the last line of `{autoresearch_log}`.
6. If `top1_val_acc` improved over `{best_before}`, keep the change.
7. If it did not improve, restore `{train_py}` to its pre-cycle contents.
8. End with a concise summary: what you tried, the result, and the keep/revert decision.

## Context

- Iteration: {iteration}
- Log rows before cycle: {log_rows_before}
- Best score before cycle: {best_before}
- Extra instructions: {extra_instructions}

## Rules

- Do exactly one experiment this cycle. Do not start a second.
- Use unsandboxed execution if needed so PyTorch can use Apple MPS.
- One change at a time -- do not bundle multiple changes.
- Keep NOTES concise and descriptive.
- Do not modify prepare.py, program.md, or the EVALUATION & LOGGING block in train.py.
