You are one cycle inside an autonomous autoresearch loop for a bird species classifier.

Read `program.md` for full instructions. Your optimization target is `top1_val_acc`.

## Scope

- Modify only `train.py`.
- Read `artifacts/autoresearch_log_full555.csv` first if it exists.
- If `artifacts/autoresearch_log_full555.csv` is missing or too short to be informative, consult
  `artifacts/logs/run_summary.csv` for historical context.

## This cycle

1. Read the experiment history to see what has been tried and what the current best is.
2. Propose exactly one experiment with a clear hypothesis.
3. Edit `train.py` -- update the CONFIGURATION section and set NOTES to describe the experiment.
4. Run `python train.py` and wait for it to finish.
5. Read the structured summary (`---` block) or the last line of `artifacts/autoresearch_log_full555.csv`.
6. If `top1_val_acc` improved over `0.894439`, keep the change.
7. If it did not improve, restore `train.py` to its pre-cycle contents.
8. End with a concise summary: what you tried, the result, and the keep/revert decision.

## Cross-reference: other species mode experiments

The subset98 dataset (29 experiments, best val_acc=0.9605) has been tested with these techniques. Use for inspiration — results may not directly correlate between dataset sizes.

Best experiment: val_acc=0.9605 — cap stage2 at 4 to extend stage3 | 1h replay

Recent experiments (status | val_acc | notes):
     keep | 0.9571 | enable CutMix | 1h replay
  discard | 0.9537 | cap stage2 at 5 to extend stage3 | 1h replay
     keep | 0.9605 | cap stage2 at 4 to extend stage3 | 1h replay
  discard | 0.9580 | cap stage1 at 5 to extend stage3 | 1h replay
  discard | 0.9528 | stage3 lr=1.5e-4 from 1e-4 | 1h replay
  discard | 0.9503 | enable focal loss gamma=1.5 | 1h replay
  discard | 0.9520 | per-stage cosine schedule on best stage caps | 1h replay
  discard | 0.9554 | bs=64 + stage3 cap=10 | 1h replay

## Context

- Iteration: 38
- Log rows before cycle: 16
- Best score before cycle: 0.894439
- Extra instructions: (none)

## Rules

- Do exactly one experiment this cycle. Do not start a second.
- Use unsandboxed execution if needed so PyTorch can use Apple MPS.
- Use one hypothesis at a time. You may change 2-3 tightly coupled parameters when needed to test a single idea, but do not bundle unrelated changes.
- Allowed examples: shorten a stage and raise that stage LR to compensate; increase batch size and adjust LR accordingly.
- Disallowed examples: change optimizer, scheduler, dropout, and stage structure all in one run.
- Keep NOTES concise and descriptive. If the experiment is coupled, mention both the primary change and the compensating adjustment.
- Do not modify prepare.py, program.md, or the EVALUATION & LOGGING block in train.py.
