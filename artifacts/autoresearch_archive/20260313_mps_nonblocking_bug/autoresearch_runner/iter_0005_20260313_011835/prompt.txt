You are one cycle inside an external autoresearch loop for this repository.

Read `program.md` and follow it. Your optimization target is the latest
`top1_val_acc` in `artifacts/autoresearch_log.csv`.

Repository-specific instructions:
- Modify only `train.py`.
- Read `artifacts/autoresearch_log.csv` first if it exists.
- If `artifacts/autoresearch_log.csv` is missing or too short to be informative, consult
  `artifacts/logs/run_summary.csv` for historical context.
- Do exactly one experiment this cycle.
- Run `python train.py` and wait for it to finish.
- Use unsandboxed execution if needed so PyTorch can use Apple MPS.
- After the run, compare the newest logged result against the best score before
  this cycle (`0.967410`).
- If the run improved on `0.967410`, keep your changes in `train.py`.
- If it did not improve, restore `train.py` to its pre-cycle contents.
- End with a concise summary of the experiment, result, and keep/revert decision.

Outer-loop context:
- Iteration: 5
- Log rows before cycle: 5
- Best score before cycle: 0.967410
- Extra instructions: (none)

Do not start a second experiment in this cycle.
